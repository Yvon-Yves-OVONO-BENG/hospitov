{% extends 'base.html.twig' %}

{% block title %}
    {% if slug == 0 %}
        {% trans %}Ajouter une prime spéciale{% endtrans %}
        {% else %}
        {% trans %}Modification d'une prime spéciale{% endtrans %}
    {% endif %}    
{% endblock %}

{% block body %}

        <!-- Row starts -->
        <div class="row gx-3">
            <div class="col-sm-12">
                <div class="card mb-3">
                    <div class="card-header">
                        <div class="row">
                            <div class="col-md-6">
                                <h5 class="card-title text-info">
                                    {% if slug == 0 %}
                                        <i class="fa fa-eercast me-2"></i>
                                        {% trans %}Ajout d'une prime spéciale{% endtrans %}
                                        {% else %}
                                        <i class="fa fa-eercast  me-2"></i>
                                        {% trans %}Modification de la prime spéciale : {% endtrans %} {{ primeSpeciale.libelle }}
                                    {% endif %}
                                </h5>
                            </div>
                            <div class="col-md-6" style="text-align: right !important;">
                                <a href="{{ path ('afficher_prime_speciale', {'a': 1 }) }}" class="btn btn-primary"><i class="fe fe-list me-2"></i>
                                    {% trans %}Afficher les prime spéciales{% endtrans %}
                                </a>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        {{ form_start(formPrimeSpeciale, {'attr': {'class':'row g-4 needs-validation', 'novalidate':""}})}}

                            <div class="row" id="primeSpecialeForm" style="margin-top: 15px !important;">
                                
                                <div class="col-md-3 position-relative">
                                    <label for="validationCustom01" class="form-label">{% trans %}Personnel{% endtrans %}</label>
                                    {{ form_widget(formPrimeSpeciale.personnel, {'attr': {'id':'validationCustom02'}}) }}

                                    <div class="invalid-feedback">
                                        {% trans %}Veuillez sélectionner !{% endtrans %}
                                    </div>
                                </div>
                                <div class="col-md-3 position-relative text-center">
                                    <label for="validationCustom01" class="form-label">{% trans %}Libellé de la prime{% endtrans %}</label>
                                    {{ form_widget(formPrimeSpeciale.libelle, {'attr': {'id':'validationCustom01'}}) }}

                                    <div class="invalid-feedback">
                                        {% trans %}Veuillez saisir le libellé de la prime spéciale !{% endtrans %}
                                    </div>
                                </div>

                                <div class="col-md-3 position-relative text-center">
                                    <label for="validationCustom01" class="form-label">{% trans %}Montant de la retenue{% endtrans %}</label>
                                    {{ form_widget(formPrimeSpeciale.montant, {'attr': {'id':'validationCustom01'}}) }}

                                    <div class="invalid-feedback">
                                        {% trans %}Veuillez saisir le montant de la prime spéciale !{% endtrans %}
                                    </div>
                                </div>

                                <input type="hidden" value="{{ csrfToken }}" name="csrfToken">

                                <div class="col-md-3 position-relative text-center mt-4">
                                   <button type="submit" class="btn btn-outline-primary btn-md">
                                        <i class="fe fe-save"></i> 
                                        {% if slug == 0 %}
                                            {% trans %}Enregistrer{% endtrans %}
                                            {% else %}
                                            {% trans %}Enregistrer les modifications{% endtrans %}
                                        {% endif %}
                                    </button>
                                </div>
                                
                            </div>

                            {# FIN PARTIE EXAMEN #}
                        {{ form_end(formPrimeSpeciale)}}
                    </div>
                </div>
            </div>
        </div>
        <!-- Row ends -->
    </div>
</div>
{% endblock %}
{% block javascripts %}

<script src="{{asset('js/choixTypePrimeSpeciale.js')}}"></script>
<script src="{{asset('js/ajoutPrimeSpeciale.js')}}"></script>
    
{% endblock %}
